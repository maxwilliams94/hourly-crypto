# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: functions-quickstart-python-azd-timer
metadata:
  template: functions-quickstart-python-azd-timer@1.0.0
services:
  api:
    project: ./src/
    language: python
    host: function